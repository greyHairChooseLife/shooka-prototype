# Changelog

## [1.7.0] - 2026-05-04

### 변경

- `/result` 페이지 모바일 반응형 개선 — 사이드바가 모바일에서 상단 세로 배치로 전환 (`md:` 이상에서 가로 레이아웃)
- 썸네일 모바일에서 전체 너비, sm 이상에서 고정 너비(`w-40`)로 전환
- 분석 목록 사이드바 접기/펼치기 토글 추가 (chevron 아이콘 + 기본 펼침)
- 사이드바 헤더에 영상 수 표시 — "분석된 영상(N)" 형식

---

## [1.6.0] - 2026-05-04

### 추가

- 이력서·자기소개서 PDF 링크 (`/resume.pdf`, `/cover-letter.pdf`) — `public/`에서 정적 서빙
- `public/*.pdf` `.gitignore` 제외 처리

---

## [1.5.0] - 2026-05-04

### 추가

- Docker 멀티스테이지 빌드 (`Dockerfile`) — deps / builder / runner 3단계, `better-sqlite3` native addon 별도 복사
- `docker-compose.yml` — app + Caddy 서비스, SQLite 영속성 볼륨
- `Caddyfile` — `$DOMAIN` 환경변수로 HTTP/HTTPS 자동 전환 (Let's Encrypt)
- `.dockerignore` 추가
- `README.md` — 서버 초기 세팅 및 운영 업데이트 가이드

### 수정

- API 클라이언트 초기화를 모듈 레벨 → 함수 내부로 이동 (Docker 빌드 타임 환경변수 오류 수정)
- 볼륨 마운트 경로 `/data` → `/app/data` 정렬 (권한 오류 수정)
- `caddy` 서비스에 `env_file` 추가 (`$DOMAIN` 미전달로 HTTPS 미적용 수정)

---

## [1.4.0] - 2026-05-04

### 추가

- `/about` 페이지 — 프로젝트 소개, 분석 파이프라인 설명, 제작자 소개 섹션
- `/about` 페이지 내 섹션 접기/펼치기(collapsible) 및 이력서 링크

### 변경

- `/` 홈을 인트로 랜딩 페이지로 전면 개편 — 서비스 소개 중심 구성
- 분석 기능을 `/analyze` 경로로 이동
- 내비게이션 순서 조정 및 UI 정리 (Footer 제거, UsageBadge 개선)
- 사전 캐시된 분석 데이터 파일 제거 (`data/cache/`)

---

## [1.3.0] - 2026-05-04

### 추가

- `/api/recent` 엔드포인트 — 최근 분석 영상 목록 반환
- `RecentVideos` 컴포넌트 — 최근 분석된 영상 카드 표시
- `useAnalyze` 공유 훅 — 분석 실행 로직을 컴포넌트에서 분리
- 분석 결과 목록에 `analyzedAt` 타임스탬프 표시
- 결과 페이지 사이드바에 분석 목록 표시 및 선택 시 해당 결과 렌더링
- 재분석 버튼 클릭 시 분석 탭으로 이동 후 즉시 새 분석 트리거

### 변경

- 분석 완료 후 `/result` 페이지로 자동 리다이렉트
- SSE 단계 메시지 문구 개선
- 액션 아이템 생성을 최대 2개(고신호 항목)로 제한 강화
- 분석 결과 캐시 조회 시 전체 결과 목록 반환 (`analyzedAt` 포함)

---

## [1.2.0] - 2026-05-03

### 추가

- SSE 진행 상황 UI 개선: 전체 단계 목록 상시 표시, 완료 단계 체크마크(✓), 진행 중 단계 스피너, 에러 시 전용 카드
- 영상 요약 단계 추가 (자막 기반 → 영상 설명 기반 fallback)
- 액션 아이템에 `sourceCommentIndices` 필드 추가 — LLM이 근거 댓글 인덱스를 직접 선택
- 근거 댓글이 없는 액션 아이템은 UI에 표시하지 않음
- `build-cache.ts`에 에러 처리 추가 (개별 영상 실패 시 skip)

### 변경

- YouTube 댓글 HTML 엔티티/태그 수집 시점에 디코딩 (`&#39;`, `<br>`, `<a>` 등)
- 댓글 분류 프롬프트: LLM이 카테고리 이름 대신 숫자(1~4)로 응답 → 서버에서 매핑 (임의 레이블 생성 방지)
- 액션 아이템 카테고리를 영어(`Topic Value` 등) → 한국어(`주제 만족도` 등)로 변경
- 액션 아이템 근거 댓글을 좋아요순 전체 표시 → LLM 선택 인덱스 기반 표시로 변경

---

## [1.1.0] - 2026-05-03

### 변경

- 댓글 분석 방법론 전면 개편: 기존 잠재피드백/표현방식 이중 분류 → 4개 카테고리 단일 분류
    - **Topic Value** — 주제·내용 만족도 반응
    - **Expertise & Correction** — 전문 지식 보완 및 사실 오류 지적
    - **Future Demand** — 다음 주제 직·간접 요청
    - **Viewer Identity** — 시청자 자기 대입·감정 표출
- 단순 리액션·감탄사 댓글은 분류 시 노이즈로 제외 (null 처리)
- 액션 아이템을 카테고리별 1~2개씩 도출하는 방식으로 변경
- 채널별 액션 프롬프트에 채널 정체성·기획 방향성 섹션 추가
- `feedbackDistribution` → `categoryDistribution`, `sourceFeedback` → `sourceCategory` 필드명 변경

### 제거

- 표현 방식 분류 단계 (classify-expression) 제거

---

## [1.0.0] - 2026-05-03

### 추가

- YouTube URL 입력 → 댓글 수집 → LLM 분석 → 액션 아이템 생성 파이프라인
- SSE(Server-Sent Events) 기반 실시간 진행 상황 스트리밍
- 잠재 피드백 분류 (콘텐츠 길이, 주제 깊이, 사실 정확성 등 8개 카테고리)
- 표현 방식 분류 (비판형, 직접요청형, 농담·밈형 등)
- 좋아요 가중 점수 기반 피드백 분포 집계
- 채널별(슈카월드·머니코믹스) 액션 아이템 생성
- SQLite 기반 분석 결과 캐시 및 일일 사용량 제한
- 사전 분석된 케이스 카드 (`/api/cases`)
- 다중 LLM 프로바이더 지원 (Claude / OpenAI / Gemini, `AI_PROVIDER` 환경변수로 전환)
- 채널별 액션 아이템 프롬프트를 마크다운 파일로 관리 (기본값: `src/prompts/defaults/`)
- `/prompts` 페이지 — 채널별 프롬프트 편집 및 DB 저장, 기본값 리셋
- 상단 탭 네비게이션 (분석 / 프롬프트)
